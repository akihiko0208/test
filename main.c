#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(void) {
        int a, b;
        char* p = malloc(100);
        printf("hello world.\n");
        if (a == 0) {
                return 1;
        }
        
        return 0;
}
